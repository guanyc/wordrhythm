# Google Analytics 4 measurement id (public, safe to commit).
# This file is intentionally NOT gitignored: Cloudflare Pages builds straight
# from git, so baking the id here means no dashboard setup is needed.
# A real environment variable (if set) still takes precedence.
NEXT_PUBLIC_GA_ID=G-X5LMS5J6QH
